[project]
name = "data-ops-inmobiliaria"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dpletzke", email = "dpletzke@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2,<3",
    "psycopg2-binary>=2.9.12",
    "python-dotenv>=1.2.3",
    "snowflake-connector-python[pandas]>=4.7.2",
]

[project.scripts]
data-ops-inmobiliaria = "data_ops_inmobiliaria:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
